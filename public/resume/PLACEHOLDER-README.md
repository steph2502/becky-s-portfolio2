# Resume / CV placeholder

The "Download CV (PDF)" button in the Contact section
(`components/Contact.tsx`) links to:

```
/resume/becky-anjola-lasore-cv.pdf
```

That file does not exist yet. To make the download button work:

1. Export Becky's resume as a PDF.
2. Save it into this folder as `becky-anjola-lasore-cv.pdf`
   (or update the `href` in `components/Contact.tsx` to match a different
   filename).
